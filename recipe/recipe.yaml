context:
  name: mupdf
  version: "1.28.3"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://www.mupdf.com/downloads/archive/mupdf-${{ version }}-source.tar.gz
  sha256: 37c3209dc0e06fa4f3781ed44839ad933a9e6143eb4731f99e069204715bcef2
  patches:
    - if: osx
      then:
        # Fix freetype lib name (-lfreetype not -lfreetype2) and remove
        # xcrun wrappers that conflict with conda compilers
        - Makerules.patch
        # Fix .so/.dylib references in the Python binding build script
        - wrap.patch
        # Python extensions must be .so even on macOS, not .dylib
        - python-ext-so.patch

build:
  number: 0
  dynamic_linking:
    missing_dso_allowlist:
      - if: osx
        then: /System/Library/Frameworks/GLUT.framework/Versions/A/GLUT

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ stdlib("c") }}
    - ${{ compiler('cxx') }}
    - if: linux
      then:
        - libgl-devel
    - if: win
      then:
        - cmake
        - ninja
    - make
    - pkg-config
    # SWIG 4.5.0 dropped the Python 2 -> 3 compatibility shims that mupdf's
    # generated bindings rely on, causing 'PyString_FromString' undeclared
    # errors. Pin below 4.5 until upstream mupdf supports SWIG 4.5.
    - swig <4.5
    - python-clang <22
  host:
    - brotli
    - python
    - pipcl
    - if: win
      then:
        - pip
        - setuptools
        - python-clang <22
    - if: linux
      then:
        - xorg-libxfixes
        - libglu
    - libgumbo
    - if: unix
      then: mesalib
    - freetype
    - jbig2dec
    - openjpeg
    - zlib
    - curl
    - harfbuzz
    - if: linux or win
      then: freeglut
    - if: linux
      then:
        - tesseract
        - leptonica
  run:
    - python
    - if: linux
      then:
        - xorg-libxext
        - xorg-libx11
    - curl

tests:
  - script:
      - if: unix
        then:
          - test -f $PREFIX/bin/mutool
          - mutool -v
      - if: win
        then:
          - if not exist %LIBRARY_BIN%\\mutool.exe exit 1
          - mutool -v
  - python:
      imports:
        - mupdf

about:
  homepage: https://www.mupdf.com/
  license: AGPL-3.0-only
  license_file: COPYING
  summary: A lightweight PDF, XPS, and E-book viewer
  description: |
    The renderer in MuPDF is tailored for high quality anti-aliased graphics.
    It renders text with metrics and spacing accurate to within fractions of
    a pixel for the highest fidelity in reproducing the look of a printed page
    on screen.
  documentation: https://www.mupdf.com/docs
  repository: http://git.ghostscript.com/?p=mupdf.git;a=summary

extra:
  recipe-maintainers:
    - xmnlab
    - scopatz
    - jan-janssen
    - benmoss
